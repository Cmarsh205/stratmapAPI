export type User = {
    id: number
    username: string
    email: string
  }

export type Stratmap = {
    id: number
    title: string
    description: string
    map: string
    createdAt: Date
    updatedAt: Date
}