export type User = {
    id: number
    name: string
    email: string
  }

export type Stratmap = {
    id: number
    title: string
    description: string
    map: string
    createdAt: Date
    updatedAt: Date
}